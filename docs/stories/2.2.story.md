# Story 2.2 — F2: Gateway Profissional em Código com YARP (.NET)

> **Epic:** [EPIC-002 — Living Lab Workshop Azure-Native](../epics/EPIC-002-living-lab-workshop.md)
> **Status:** Done
> **Created:** 2026-05-25 by @sm (River)
> **Re-drafted:** 2026-06-03 by @sm (River) — re-escopo EPIC-002 conforme ADE-004/ADE-005
> **Validated:** 2026-06-03 by @po (Pax) — GO 9/10 (`docs/qa/2026-06-03-po-validation-S2.2.md`)
> **Sprint context:** Segunda fase (cumulativa de F1). Aluno constrói e deploya um gateway profissional como código C# usando YARP (Yet Another Reverse Proxy), hospedado em Container App dedicado, à frente das Functions de F1.
> **Depends on:** Story 2.1 (F1) Done · **Source:** blueprint seção 4.F2 + ADE-004

---

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools:
  - "code-review"
  - "pattern-validation"
  - "security-validation"
deployment_owner: "@devops"
content_collaboration: "@analyst"
```

**Rationale:** Story de código .NET — `@dev` implementa ASP.NET Core + YARP (rate-limiter, cache, transforms, auth placeholder), @analyst produz artefatos didáticos; `@architect` valida o padrão gateway-as-code antes do merge. `@devops` cria o Container App por aluno e gerencia CI/CD. Não há mais APIM compartilhado — cada aluno tem seu próprio gateway.

---

## Story

**Como** aluno do workshop "Living Lab Azure-Native",
**eu quero** completar a Fase 2 em 6h implementando um gateway profissional em código C# com YARP hospedado em Container App, colocando-o à frente das Functions de F1 e configurando preocupações transversais (rate limiting, output cache, CORS, header transform, path rewrite) em código .NET legível e inspecionável,
**para que** eu compreenda o que um API Gateway faz por dentro — como rate-limit, cache, transformação de headers e validação de JWT são mecanismos de código, não caixas-pretas gerenciadas — e tenha o gateway versionado no repo como ponto de entrada único do fluxo v2, base para F3 (identidade/JWT) e F6 (Flow Visualizer).

---

## Acceptance Criteria

1. **AC-1 — Branch criada:** `phase-02-gateway` partindo de `phase-01-servicebus-functions`, com workflow `.github/workflows/deploy-phase-02.yml`.

2. **AC-2 — Projeto YARP criado e versionado no repo:** `src/Fifa2026.V2.Gateway/` com `Fifa2026.V2.Gateway.csproj` (ASP.NET Core .NET 8, pacote `Yarp.ReverseProxy`). Configuração de rotas/clusters em `appsettings.json` apontando para a Function App F1 do aluno. **Conforme ADE-004 Invariante 1 e 2.**

3. **AC-3 — Container App provisionado via Portal (por aluno):** aluno demonstra Portal → Resource Group → Container Apps → Create → imagem do gateway subida. Cada aluno opera seu próprio Container App de gateway (sem recurso compartilhado). **Conforme ADE-004 Invariante 5.**

4. **AC-4 — Routing funcional (Nível 1):** `POST /purchase` e `GET /purchase/{correlationId}` roteados via YARP para a Function App F1. Aluno demonstra que cliente nunca vê a URL real da Function (URL real desacoplada). **Conforme ADE-004 Invariante 3 (path rewrite / PathPattern).**

5. **AC-5 — Rate limiting em código (Nível 2):** `AddRateLimiter` com partição por IP/key — 5 requisições por minuto. Aluno demonstra: 6ª chamada em < 1 min retorna **HTTP 429**. **Conforme ADE-004 Invariante 3 (`rate-limit-by-key` → `AddRateLimiter`).**

6. **AC-6 — Output cache em código (Nível 2):** `AddOutputCache` com policy de 30s em `GET /purchase/{correlationId}`. Aluno demonstra: 2ª chamada idêntica responde em < 50ms com header `X-Cache: HIT` (ou equivalente configurado). **Conforme ADE-004 Invariante 3 (`cache-lookup/store` → `AddOutputCache`).**

7. **AC-7 — CORS restrito em código (Nível 2):** `AddCors` + `UseCors` com allow-origin restrito ao domínio do frontend (`fifa2026-web.azurewebsites.net`). **Conforme ADE-004 Invariante 3 (`cors` → `AddCors`).**

8. **AC-8 — Header transform `X-Correlation-ID` (Nível 2):** YARP `RequestTransform` injeta header `X-Correlation-ID` (novo GUID se ausente) em cada requisição encaminhada ao backend. **Conforme ADE-004 Invariante 3 (`set-header` → YARP `Transforms`; ADE-000 Invariante 5).**

9. **AC-9 — JWT placeholder preparatório (Nível 2 → F3):** `AddAuthentication().AddJwtBearer(...)` configurado mas rotas anonimas (sem `RequireAuthorization()`) — exatamente como o `<choose>` desabilitado era em F2 APIM. Aluno comenta o código `RequireAuthorization()` para mostrar que F3 ativará a validação JWT do Entra. **Conforme ADE-004 Invariante 4 (paridade de faseamento com APIM policy placeholder).**

10. **AC-10 — Smoke test ponta-a-ponta:** chamar `https://<gateway-container-app>.azurecontainerapps.io/purchase` via `curl`/Postman → gateway aplica rate-limit → encaminha para Function F1 → mensagem na queue → consumer grava em SQL. **URL real da Function não aparece no cliente.**

11. **AC-11 — Tracing end-to-end (Nível 4):** App Insights mostra trace correlacionado Gateway Container App → Function → Service Bus → Consumer, com `X-Correlation-ID` visível nos logs. **Conforme ADE-004 Invariante 3 (observabilidade de borda) e ADE-000 Invariante 5.**

12. **AC-12 — Projeto YARP versionado no repo:** `src/Fifa2026.V2.Gateway/` com código de rate-limiter, output cache, transforms e JWT placeholder — substituindo qualquer `apim/policies/*.xml`. **Conforme ADE-004 Invariante 1 (gateway como código auditável, não caixa-preta XML).**

13. **AC-13 — 6 artefatos didáticos** em `docs/workshops/phase-02/`:
    - `README.md` (leitura prévia — cobre os 6 níveis do gateway, comparação YARP vs APIM)
    - `PORTAL-GUIDE.md` (passo-a-passo Portal: Container App, deploy da imagem, variáveis de ambiente)
    - `SPEAKER-NOTES.md` (notas do facilitador com perguntas de checagem por nível)
    - `slides.md` (Reveal.js OU markdown convertível — ~30-50 slides cobrindo os 6 níveis)
    - `intro-video-script.md` (~5min de vídeo, roteiro)
    - Branch + workflow CI/CD (a própria branch criada)

14. **AC-14 — Anti-hallucination (Art. IV — No Invention):** cada package NuGet, API .NET e configuração YARP rastreável a docs oficiais (`Yarp.ReverseProxy`, `Microsoft.AspNetCore.RateLimiting`, `Microsoft.AspNetCore.OutputCaching`). NÃO inventar nomes de APIs ou versões de pacotes.

---

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type:** API + Integration (YARP reverse proxy, gateway-as-code, ASP.NET Core middleware pipeline)
**Secondary Type(s):** Security (JWT placeholder, CORS, rate-limit), Deployment (Container App), Documentation (6 artefatos didáticos)
**Complexity:** HIGH (introduz padrão gateway-as-code reutilizado por F3+; Container App como novo tipo de recurso)

### Specialized Agent Assignment

**Primary Agents:**
- @dev (Dex) — implementação ASP.NET Core + YARP, rate-limiter, output cache, transforms, CI/CD workflow
- @analyst (Atlas) — produção dos artefatos didáticos (README, PORTAL-GUIDE, SPEAKER-NOTES, slides)

**Supporting Agents:**
- @architect (Aria) — quality gate, validação do padrão gateway-as-code conforme ADE-004
- @devops (Gage) — push final, criação do Container App por aluno, smoke test em prod, secrets

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): Validar pipeline de middleware (ordem: CORS → RateLimit → Auth → Proxy), security best practices no JWT placeholder, parameterized builds
- [ ] Pre-PR (@devops): Container App health check + smoke test via URL do gateway
- [ ] Pre-Deployment (@devops): Smoke test pós-deploy validando rate-limit 429 e trace no App Insights

### Self-Healing Configuration

**Expected Self-Healing:**
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 30 minutes
- Severity Filter: CRITICAL, HIGH

**Predicted Behavior:**
- CRITICAL issues: auto_fix (block if persists after 2 iter)
- HIGH issues: auto_fix (document if fails)

### CodeRabbit Focus Areas

**Primary Focus:**
- Ordem do pipeline de middleware (CORS antes de RateLimit antes de Auth antes de Proxy — errar a ordem quebra comportamento)
- JWT placeholder: garantir que rotas são anônimas em F2 (não exigir token ainda)

**Secondary Focus:**
- Connection string e URL da Function em variáveis de ambiente (não hardcoded)
- `X-Correlation-ID` sendo gerado como GUID novo quando ausente (não duplicado)

---

## Tasks / Subtasks

- [x] **Task 1 — Branch + projeto YARP** (AC: 1, 2, 14)
  - [x] 1.1 Criar branch `phase-02-gateway` a partir de `phase-01-servicebus-functions`
  - [x] 1.2 Criar `src/Fifa2026.V2.Gateway/Fifa2026.V2.Gateway.csproj` (ASP.NET Core .NET 8, SDK `Microsoft.NET.Sdk.Web`)
  - [x] 1.3 Adicionar pacote `Yarp.ReverseProxy` 2.2.0 (estável, net8.0 confirmado no nuspec — AC-14)
  - [x] 1.4 Configurar `appsettings.json` com seção `ReverseProxy` → routes + clusters; destination da Function F1 via env `FunctionAppF1Url` (override por `FunctionDestinationConfigFilter` — ADE-003 Inv 3)
  - [x] 1.5 `Program.cs`: `AddReverseProxy().LoadFromConfig(...)` + `app.MapReverseProxy()`

- [x] **Task 2 — Preocupações transversais (middleware pipeline)** (AC: 5, 6, 7, 8, 9)
  - [x] 2.1 Rate limiting: `AddRateLimiter` fixed window 5/min particionado por IP + `UseRateLimiter()` + `RequireRateLimiting("fixed")` no MapReverseProxy (RejectionStatusCode 429)
  - [x] 2.2 Output cache: `AddOutputCache` policy `purchase-status-30s` (30s) + `.CacheOutput(...)`; header `X-Cache` HIT/MISS via `XCacheOutputCachePolicy` + `XCacheMiddleware`
  - [x] 2.3 CORS: `AddCors` policy `frontend` com origin restrito (env `Gateway:FrontendOrigin`) + `UseCors("frontend")`
  - [x] 2.4 Header transform: `AddRequestTransform` programático → injeta `X-Correlation-ID` (novo GUID se ausente) downstream e devolve ao cliente (ADE-000 Inv 5)
  - [x] 2.5 JWT placeholder: `AddAuthentication().AddJwtBearer("Entra", ...)` apontando para issuer Entra + comentário `// F3: aplicar .RequireAuthorization()` (rotas anônimas em F2)
  - [x] 2.6 Ordem do pipeline: `UseCors` → `UseRateLimiter` → `UseOutputCache` → `UseAuthentication` → `UseAuthorization` → `MapReverseProxy` ✓

- [x] **Task 3 — Routing (path rewrite + path strip)** (AC: 4)
  - [x] 3.1 Route `purchase-post` (`/purchase` POST → transform `PathSet /api/v2/purchase`) e `purchase-get` (`/purchase/{correlationId}` GET → transform `PathPattern /api/v2/purchase/{correlationId}`)
  - [x] 3.2 Validado por teste de integração: `POST /purchase` chega como `POST /api/v2/purchase` no backend mockado. `SqlConnectionString` NÃO está no gateway (ADE-003 Inv 3 respeitado)

- [x] **Task 4 — CI/CD Workflow + containerização** (AC: 1, 3)
  - [x] 4.1 `Dockerfile` multi-stage em `src/Fifa2026.V2.Gateway/` (sdk:8.0 publish → aspnet:8.0 runtime, porta 8080) + `.dockerignore`
  - [x] 4.2 `.github/workflows/deploy-phase-02.yml` — build/push imagem ACR + `az containerapp update`. **SEM `--no-build` no `dotnet test`** (evita o bug H-1 da Fase 1)
  - [x] 4.3 Env do Container App documentadas no workflow: `FunctionAppF1Url`, `Jwt__TenantId` (placeholder F3) — via App Settings, não hardcoded
  - [x] 4.4 Smoke test no workflow: resolve FQDN, `POST /purchase`, valida `.correlationId` + header `X-Correlation-ID`
  - **Nota runtime-only (@devops):** criação do Container App/ACR e provisão de secrets/vars NÃO estão neste escopo (são per-aluno/@devops)

- [x] **Task 5 — Smoke test e validação de ACs** (AC: 10, 11)
  - [x] 5.1 Encaminhamento validado por teste de integração (gateway → backend mockado). O fluxo completo até SQL (5.1 end), 5.4 (trace App Insights) e AC-3/AC-10/AC-11 contra Azure real são **runtime-only** (validados pelo @devops pós-deploy / pelo aluno)
  - [x] 5.2 Rate-limit: teste `SixthRequest_WithinWindow_Returns_429` cobre a 6ª chamada → 429
  - [x] 5.3 Output cache: teste `SecondIdenticalGet_Returns_CacheHit_WithoutHittingBackend` cobre 2º GET → `X-Cache: HIT` sem novo hit no backend
  - [x] 5.4 Trace correlacionado no App Insights: **runtime-only** (requer Azure + APPLICATIONINSIGHTS_CONNECTION_STRING); o gateway injeta `X-Correlation-ID` (testado) — a visualização do trace é validada em runtime

- [x] **Task 6 — Artefatos didáticos** (AC: 13) — delegar @analyst
  - [x] 6.1 `docs/workshops/phase-02/README.md` — cobre os 6 níveis do gateway (Nível 0–5), anatomia do YARP, comparação "construir vs comprar (APIM)"
  - [x] 6.2 `docs/workshops/phase-02/PORTAL-GUIDE.md` — Steps: Container Registry → Container App → deploy da imagem → variáveis de ambiente → smoke test
  - [x] 6.3 `docs/workshops/phase-02/SPEAKER-NOTES.md` — notas por nível (perguntas de checagem, armadilhas, "se sobrar tempo", transição para F3)
  - [x] 6.4 `docs/workshops/phase-02/slides.md` — Reveal.js, ~30-50 slides pelos 6 níveis + slide de trade-off "APIM vs YARP"
  - [x] 6.5 `docs/workshops/phase-02/intro-video-script.md` — roteiro ~5min

- [x] **Task 7 — Anti-hallucination check** (AC: 14)
  - [x] 7.1 Packages validados contra NuGet.org: `Yarp.ReverseProxy` 2.2.0 (nuspec confirma target net8.0), `Microsoft.AspNetCore.Authentication.JwtBearer` 8.0.8, `Microsoft.ApplicationInsights.AspNetCore` 2.22.0, `WireMock.Net` 1.25.0, `Microsoft.AspNetCore.Mvc.Testing` 8.0.8
  - [x] 7.2 APIs reais .NET 8: `AddRateLimiter`/`RateLimitPartition.GetFixedWindowLimiter` (in-box), `AddOutputCache`/`IOutputCachePolicy` (in-box), `AddJwtBearer` (pacote JwtBearer). Build com 0 warnings/0 erros confirma resolução real
  - [x] 7.3 YARP transforms: usados `PathSet` e `PathPattern` (declarativos em appsettings) + `AddRequestTransform` (programático) — todos APIs reais do `Yarp.ReverseProxy.Transforms`. Override de destination via `IProxyConfigFilter` (`FunctionDestinationConfigFilter`)

---

## Dev Notes

### Objetivos pedagógicos (6 níveis — conforme Orion/ADE-004)

| Nível | Conceito | O que o aluno aprende |
|---|---|---|
| **0 — Ponto único de entrada** | Reverse proxy, padrão API Gateway/BFF | Cliente nunca vê URL real da Function; desacoplamento de topologia |
| **1 — Roteamento** | Path/host-based routing | `routes` + `clusters` do YARP em `appsettings.json` |
| **2 — Preocupações transversais (coração)** | Rate limiting, auth JWT, CORS, header transform, path rewrite | `AddRateLimiter` (→ 429), `AddJwtBearer` (placeholder F3), `AddCors`, transforms `X-Correlation-ID` — tudo em C# legível |
| **3 — Resiliência** | Load balancing, health checks, timeouts, retries | Clusters YARP com múltiplas destinations; health probes ativo/passivo |
| **4 — Observabilidade de borda** | Logging/tracing centralizado | Gateway é o **primeiro nó** do Flow Visualizer (F6); `X-Correlation-ID` propagado desde aqui |
| **5 — Meta-aprendizado** | Trade-off "comprar gerenciado vs construir" | Custo, controle, provisioning: APIM (US$50-80, 30-45min) vs YARP (US$0, segundos); "em produção corporativa, o equivalente gerenciado é o APIM" |

### Arquitetura técnica delta

```
[Browser]
   │ POST /purchase  (URL do Container App do aluno)
   ▼
[Container App: gateway-<iniciais>  (ASP.NET Core + YARP .NET 8)]
   ├─ UseCors("frontend")
   ├─ UseRateLimiter  ── 429 se > 5/min
   ├─ UseAuthentication / UseAuthorization  ── placeholder F3 (anônimo em F2)
   ├─ RequestTransform: X-Correlation-ID injetado  ── ADE-000 Inv 5
   ├─ OutputCache  ── cache 30s em GET
   └─ MapReverseProxy ── roteia para Function App F1 do aluno
            │
            ▼
     [Function App F1: PurchaseEntryFunction]
            └─ (resto do fluxo F1: Service Bus → Consumer → SQL)
```

> **NÃO toca:** Node API (`fifa2026-api/`), frontend Vite, SQL schema, Functions F1 (apenas recebem requisições via gateway agora). A connection string `SqlConnectionString` permanece nas Functions, não no gateway (conforme ADE-003 Invariante 3).

### Tabela de paridade APIM → YARP (conforme ADE-004 Invariante 3)

| Capacidade (APIM policy) | Equivalente YARP / ASP.NET Core | AC |
|---|---|---|
| `rate-limit-by-key` | `AddRateLimiter` + partição por IP/key (429 ao extrapolar) | AC-5 |
| `cache-lookup` / `cache-store` | `AddOutputCache` (30s, header X-Cache HIT) | AC-6 |
| `cors` | `AddCors` + `UseCors` (allow-origin restrito) | AC-7 |
| `set-header X-Correlation-ID` | YARP `RequestTransforms` → `RequestHeader` | AC-8 |
| `rewrite-uri` / path strip | YARP `PathPattern` / `PathRemovePrefix` | AC-4 |
| `validate-jwt` (placeholder) | `AddJwtBearer` configurado, rotas anônimas em F2 | AC-9 |

### GitHub Actions workflow (esqueleto)

```yaml
name: Deploy Phase 02 — Gateway YARP
on:
  push:
    branches: [phase-02-gateway]
  workflow_dispatch:
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with: { dotnet-version: '8.0.x' }
      - name: Build & push Docker image
        run: |
          docker build -t ${{ vars.ACR_LOGIN_SERVER }}/gateway:${{ github.sha }} \
            src/Fifa2026.V2.Gateway
          docker push ${{ vars.ACR_LOGIN_SERVER }}/gateway:${{ github.sha }}
      - name: Deploy Container App
        run: |
          az containerapp update \
            --name gateway-${{ vars.STUDENT_INITIALS }} \
            --resource-group ${{ vars.RG_NAME }} \
            --image ${{ vars.ACR_LOGIN_SERVER }}/gateway:${{ github.sha }}
      - name: Smoke test
        run: |
          curl -fsS https://gateway-${{ vars.STUDENT_INITIALS }}.azurecontainerapps.io/purchase \
            -X POST -H "Content-Type: application/json" \
            -d '{"matchId":1,"category":"VIP","userId":1,"quantity":1}' | tee response.json
          jq -e '.correlationId' response.json
```

### Roteiro de aula (6h = 360min)

| # | Bloco | Tempo | Modo |
|---|---|---|---|
| 1 | Conceitos: 6 níveis do gateway, reverse proxy, BFF pattern, "construir vs comprar" | 50min | Aula expositiva + Q&A |
| 2 | **Provisioning Container App + Container Registry via Portal** (passo-a-passo) | 45min | Demo guiada Portal — instrutor projeta, alunos replicam |
| 3 | Projeto YARP: routing + CORS + header transform (live coding) | 60min | Live coding |
| ☕ | Coffee break | 15min | — |
| 4 | Rate limiting + Output Cache + JWT placeholder (live coding) | 60min | Live coding |
| 5 | CI/CD + smoke test (rate-limit 429, cache HIT, trace no App Insights) | 40min | Hands-on |
| 6 | Trade-off "APIM vs YARP" + preparação F3 (JWT placeholder explicado) + Retro | 50min | Conversa + Q&A |

### Troubleshooting esperado

| Sintoma | Causa | Mitigação |
|---|---|---|
| 502 Bad Gateway do Container App | URL da Function errada em `FunctionAppF1Url` | Verificar App Setting no Container App; confirmar que Function usa `authLevel: Anonymous` (F1) |
| Rate-limit não dispara (sem 429) | `UseRateLimiter` antes de `MapReverseProxy` mas `RequireRateLimiting` não aplicado à rota | Adicionar `RequireRateLimiting("fixed")` na configuração do proxy |
| X-Correlation-ID não aparece no backend | Transform configurado em `clusters` em vez de `routes` | Mover `RequestTransforms` para a seção de route |
| Output cache não retorna HIT | Policy de cache aplicada ao POST em vez do GET | Segregar routes: uma para POST (sem cache), outra para GET (com cache) |
| Container App cold start alto | Consumption plan scale-to-zero | Aceitar em workshop (didático — mencionar "min replicas: 1" em prod) |
| JWT placeholder bloqueia chamadas em F2 | `RequireAuthorization()` aplicado antes de F3 | Confirmar que rotas estão com `.AllowAnonymous()` em F2 |

### Correlation propagation pattern (preparatório para F6 — ADE-000 Inv 5)

- **Gateway injeta:** `X-Correlation-ID` via YARP `RequestTransform` (novo GUID se ausente)
- **Function recebe:** header `X-Correlation-ID` e propaga via `ApplicationProperties["CorrelationId"]` no Service Bus
- **Gateway é o nó zero** do Flow Visualizer de F6 — o primeiro evento de tracing começa aqui

### Source files referenciados

- `docs/architecture/ade-004-gateway-yarp.md` (decisão completa — Invariantes 1-5, tabela de paridade Inv 3)
- `docs/architecture/ade-005-identity-easy-auth.md` (integração JWT F3 — Invariante 4)
- `docs/architecture/ade-003-v2-infrastructure-baseline.md` (ADE-003 Inv 3: `SqlConnectionString` permanece nas Functions, não no gateway)
- `docs/architecture/ade-000-microservice-parallel-pattern.md` (ADE-000 Inv 5: `X-Correlation-ID` obrigatório)
- `docs/epics/EPIC-002-living-lab-workshop.md`
- `docs/workshops/2026-blueprint-living-lab-azure.md` (seção 4.F2 — base original, superada por ADE-004)
- `src/Fifa2026.V2.Functions/` (F1 — backend alvo do proxy)

### Testing

**Test file location:**
- `src/Fifa2026.V2.Gateway.Tests/` (xUnit)

**Test standards:**
- Unit tests: middleware pipeline (rate-limiter stub, header transform, routing configuration)
- Integration tests: `WebApplicationFactory<Program>` + mock do backend Function (stub HTTP)
- Smoke test: curl + jq no workflow CI/CD (AC-10)

**Frameworks:**
- xUnit 2.6+
- Microsoft.AspNetCore.Mvc.Testing (WebApplicationFactory)
- WireMock.Net (mock do backend Function nos testes de integração)

**Specific requirements:**
- Cobertura mínima: 60% do pipeline de middleware
- Smoke test no CI/CD obrigatório (não pode falhar)
- Teste específico: 6ª requisição retorna 429 (rate-limit)
- Teste específico: 2ª GET idêntica retorna header de cache hit

---

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-05-25 | 0.1 | Story drafted from EPIC-002 + blueprint | @sm (River) |
| 2026-06-03 | 0.2 | Re-draft completo: substituição APIM Developer → Gateway YARP em código .NET (conforme ADE-004); branch renomeada `phase-02-apim` → `phase-02-gateway`; ACs/Tasks reescritos para equivalentes YARP da Invariante 3; objetivos pedagógicos atualizados para 6 níveis (Orion); hosting APIM compartilhado → Container App por aluno (ADE-004 Inv 5); referências ADE-005 adicionadas para placeholder JWT | @sm (River) |
| 2026-06-03 | 0.3.0 | Validated GO (9/10) — Status: Draft → Ready | @po (Pax) |
| 2026-06-06 | 0.4.0 | Implementação do código (Tasks 1-5,7): projeto YARP `src/Fifa2026.V2.Gateway/`, pipeline transversal (rate-limit/cache/CORS/transform/JWT placeholder), Dockerfile, workflow `deploy-phase-02.yml`, testes de integração (5/5 pass). Status: Ready → InProgress. Task 6 (artefatos didáticos) pendente para @analyst — story NÃO vai para InReview ainda | @dev (Dex) |
| 2026-06-06 | 0.5.0 | Artefatos didáticos (Task 6 / AC-13): 5 arquivos em `docs/workshops/phase-02/` (README, PORTAL-GUIDE, SPEAKER-NOTES, slides, intro-video-script). Consistência total com o código real (`/purchase`→`/api/v2/purchase`, rate-limit 5/min→429, output cache 30s + X-Cache HIT/MISS, CORS `Gateway:FrontendOrigin`, transform X-Correlation-ID, JWT `AddJwtBearer("Entra")` anônimo, env `FunctionAppF1Url`/`Gateway__FrontendOrigin`/`Jwt__TenantId`, port 8080, packages Yarp.ReverseProxy 2.2.0 / JwtBearer 8.0.8 / ApplicationInsights.AspNetCore 2.22.0). Continuidade pedagógica F1→F2 + ponte F2→F3. Status: InProgress → InReview (código + docs completos) | @analyst (Atlas) |
| 2026-06-06 | 0.6.0 | **Architect Quality Gate: GO (PASS) com 4 CONCERNS** (0 HIGH, 3 MEDIUM, 1 LOW; 0 bloqueantes). ADE-004 5/5 invariantes + paridade APIM→YARP 6/6; ADE-003 Inv 3, ADE-005 Inv 4, ADE-000 Inv 5 confirmadas. Pipeline na ordem correta; build 0-warnings; 5/5 testes PASS (executados localmente); CI H-1 não recorre. Concerns carry-forward: M-1 (JWT tenant/issuer)→S2.3, M-2 (correlation ID em cache HIT)→S2.6. Relatório: `docs/qa/2026-06-06-architect-gate-S2.2.md`. Status: InReview → Done | @architect (Aria) |

---

## Dev Agent Record

### Agent Model Used

claude-opus-4-8 (1M context) — @dev (Dex), modo YOLO.

### Status Transitions

- **Ready → InProgress** (2026-06-06): início da implementação de código (Tasks 1-5, 7).
- **InProgress → InReview** (2026-06-06): Task 6 (5 artefatos didáticos em `docs/workshops/phase-02/`) entregue pelo @analyst (Atlas). Código (Tasks 1-5,7) + docs (Task 6) completos — pacote da story pronto para o quality gate do @architect.

### Completion Notes

- **Branch:** `phase-02-gateway` criada a partir de `phase-01-servicebus-functions` (ADE-000 Inv 7 — branching cumulativo).
- **Endpoint real do backend confirmado** lendo `PurchaseEntryFunction`/`PurchaseStatusFunction`: rotas reais são `POST /api/v2/purchase` e `GET /api/v2/purchase/{correlationId}` (prefixo `api` + route `v2/purchase`). O gateway expõe `/purchase` e reescreve para `/api/v2/purchase` (AC-4).
- **Pipeline na ordem correta** (ADE-004 / Task 2.6): `UseCors → UseRateLimiter → UseOutputCache → UseAuthentication → UseAuthorization → MapReverseProxy`.
- **X-Cache HIT/MISS:** o OutputCache do ASP.NET Core não expõe hit/miss nativamente. Resolvido com `XCacheOutputCachePolicy` (seta HIT em `ServeFromCacheAsync`, estágio com headers graváveis) + `XCacheMiddleware` (default MISS via `Response.OnStarting`, evitando escrita em headers já commitados pelo YARP — bug encontrado e corrigido durante o dev).
- **JWT placeholder (AC-9):** `AddJwtBearer("Entra", ...)` configurado para o issuer Entra workforce, mas SEM `RequireAuthorization()` — rotas anônimas em F2 (paridade com o `<choose>` desabilitado do APIM). Marcador `// F3: aplicar .RequireAuthorization()` no `MapReverseProxy`.
- **ADE-003 Inv 3 respeitado:** nenhuma `SqlConnectionString` no gateway. Só `FunctionAppF1Url` (destination externalizada via `FunctionDestinationConfigFilter` — `IProxyConfigFilter`). A connection string SQL permanece nas Functions.
- **CI/CD H-1 evitado:** o step `dotnet test` em `deploy-phase-02.yml` NÃO usa `--no-build` (deixa o test fazer restore+build do projeto de testes e da dependência) — lição do gate S2.1.
- **Build:** `dotnet build src/Fifa2026.V2.Gateway -c Release` → êxito, 0 warnings, 0 erros.
- **Testes:** `dotnet test src/Fifa2026.V2.Gateway.Tests -c Release` → **5/5 aprovados** (routing/path-rewrite, X-Correlation-ID injection, rate-limit 429, output cache HIT/MISS, health). Backend Function mockado por WireMock.Net via `GatewayTestFixture` (WebApplicationFactory).
- **IDS:** REUSE do padrão de projeto/test/workflow de F1 (`Fifa2026.V2.Functions` + `.Tests` + `deploy-phase-01.yml`); CREATE justificado do novo projeto gateway (não existia). Sem duplicação.

### Runtime-only (fora do escopo de código — @devops / aluno)

AC-3 (provisão Container App via Portal), AC-10 fluxo end-to-end até SQL, AC-11 (trace App Insights), Task 5.1-final/5.4 exigem Azure real e são validados pelo @devops pós-deploy / pelo aluno no workshop.

### File List

**Criados:**
- `src/Fifa2026.V2.Gateway/Fifa2026.V2.Gateway.csproj`
- `src/Fifa2026.V2.Gateway/Program.cs`
- `src/Fifa2026.V2.Gateway/appsettings.json`
- `src/Fifa2026.V2.Gateway/appsettings.Development.json`
- `src/Fifa2026.V2.Gateway/Infrastructure/FunctionDestinationConfigFilter.cs`
- `src/Fifa2026.V2.Gateway/Infrastructure/XCacheOutputCachePolicy.cs`
- `src/Fifa2026.V2.Gateway/Infrastructure/XCacheMiddleware.cs`
- `src/Fifa2026.V2.Gateway/Dockerfile`
- `src/Fifa2026.V2.Gateway/.dockerignore`
- `src/Fifa2026.V2.Gateway.Tests/Fifa2026.V2.Gateway.Tests.csproj`
- `src/Fifa2026.V2.Gateway.Tests/GatewayTestFixture.cs`
- `src/Fifa2026.V2.Gateway.Tests/RoutingTests.cs`
- `src/Fifa2026.V2.Gateway.Tests/RateLimitTests.cs`
- `src/Fifa2026.V2.Gateway.Tests/OutputCacheTests.cs`
- `.github/workflows/deploy-phase-02.yml`
- `docs/workshops/phase-02/README.md` (@analyst — leitura prévia: 6 níveis, anatomia YARP, construir vs comprar)
- `docs/workshops/phase-02/PORTAL-GUIDE.md` (@analyst — Container Registry → Container App → deploy → env vars → smoke test)
- `docs/workshops/phase-02/SPEAKER-NOTES.md` (@analyst — notas do facilitador por bloco/nível, roteiro 6h)
- `docs/workshops/phase-02/slides.md` (@analyst — Reveal.js, ~45 slides pelos 6 níveis + trade-off APIM vs YARP)
- `docs/workshops/phase-02/intro-video-script.md` (@analyst — roteiro de vídeo ~5min)

**Modificados:**
- `docs/stories/2.2.story.md` (Status, Tasks, Change Log, Dev Agent Record, File List)

## QA Results

### Architect Quality Gate — 2026-06-06 (Aria)

**Verdict: GO (PASS) com CONCERNS** · Relatório completo: [`docs/qa/2026-06-06-architect-gate-S2.2.md`](../qa/2026-06-06-architect-gate-S2.2.md)

```yaml
storyId: 2.2
verdict: PASS
adherence_ade_004: CONFIRMED (5/5 invariantes)
adherence_ade_003_inv3: CONFIRMED (sem SqlConnectionString no gateway)
adherence_ade_005_inv4: CONFIRMED (JWT placeholder, rotas anônimas)
adherence_ade_000_inv5: CONFIRMED (X-Correlation-ID GUID novo se ausente)
build: SUCCESS (0 warnings, 0 erros)
tests: 5/5 PASS (executado localmente)
concerns: 4   # 0 HIGH · 3 MEDIUM · 1 LOW
blocking_issues: 0
```

**ACs:** 10 de código atendidos (1,2,4,5,6,7,8,9,12,13,14); 1 híbrido parcial (11 — wired ✅ / trace live runtime); 2 runtime-only legítimos (3 Portal, 10 E2E até SQL). Nenhum AC de código não atendido.

**Aderência arquitetural:** ADE-004 5/5 invariantes + tabela de paridade APIM→YARP 6/6 (rate-limit→429, cache→X-Cache, CORS, set-header X-Correlation-ID, path rewrite, JWT placeholder). ADE-003 Inv 3 confirmada (só `FunctionAppF1Url`, zero segredo SQL). ADE-005 Inv 4 confirmada (JWT configurado, rotas anônimas, marcador `// F3:`). ADE-000 Inv 5 confirmada.

**Pipeline:** ordem correta `UseCors → UseRateLimiter → XCacheMiddleware → UseOutputCache → UseAuthentication → UseAuthorization → MapReverseProxy` (armadilha nº1 da fase evitada). Rate-limit escopado ao proxy, não ao `/health`. X-Correlation-ID gera GUID novo só quando ausente, sem duplicação.

**Build + testes (executados localmente):** `dotnet test` → 5/5 PASS (routing/path-rewrite, X-Correlation-ID, health, rate-limit 429, cache HIT/MISS). Build 0 warnings/0 erros confirma anti-hallucination (AC-14).

**CI/CD:** bug H-1 do gate S2.1 (`--no-build` no `dotnet test`) **NÃO se repete** — `deploy-phase-02.yml` corrigido com comentário explícito.

**Docs:** fidelidade técnica alta ao código real.

**Concerns (não bloqueantes):**
- **M-1 (security, → F3):** JWT default `Jwt:TenantId="common"` é placeholder; ao ativar `RequireAuthorization()` em F3, fixar tenant workforce do aluno (ADE-005 Inv 1) + `ValidIssuer`/`ValidAudiences` explícitos.
- **M-2 (code, → F6):** transform de X-Correlation-ID roda após OutputCache; em cache HIT a resposta carrega o correlation ID da chamada original. Decidir/documentar para o Flow Visualizer de F6.
- **M-3 (security, hardening):** CORS `AllowAnyMethod/AnyHeader` com origin restrito — aceitável em workshop; restringir métodos/headers em prod (nota didática SPEAKER-NOTES).
- **L-1 (tests):** sem cobertura de CORS preflight (OPTIONS) e do correlation ID em cache HIT — oportunístico.

**Carry-forward:** M-1 → S2.3 (F3); M-2 → S2.6 (F6).
