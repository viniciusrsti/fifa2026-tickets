# Story 2.9 — Code Review Manual (@dev, em substituição ao CodeRabbit)

**Data:** 2026-06-11
**Revisor:** @dev (Dex) — review manual autorizada pelo owner após falha do CodeRabbit
**Escopo:** diff completo uncommitted da S2.9 (12 arquivos, +793/−49)

## Por que manual (graceful degradation)

CodeRabbit CLI 0.6.0 (WSL Ubuntu) **estagnou duas vezes** sem emitir nenhum finding:
1ª execução pendurada por ~3,5h (morta via SIGTERM); 2ª execução com watchdog
`timeout 540` também sem progresso (morta via SIGKILL). `coderabbit auth status`
OK, porém **Plan: Free / Seat: not assigned** — causa provável: review presa em
fila server-side. Conforme `core-config.yaml > coderabbit_integration >
graceful_degradation`, a review não bloqueia o fluxo; o owner instruiu
explicitamente a review manual pelo agente.

**Ação recomendada (infra):** atribuir seat no CodeRabbit ou ajustar o plano —
sem isso o self-healing loop do SDC fica inoperante (recorrência).

## Findings

| # | Severidade | Arquivo | Finding | Ação |
|---|-----------|---------|---------|------|
| 1 | LOW | `Data/AlertWebhookNotifier.cs` | `CreateClient()` sem `using` (paridade com o code sample da story; dispose do wrapper é barato e não afeta o handler pooled) | **CORRIGIDO** (`using var client`) |
| 2 | INFO | `Data/AlertWebhookNotifier.cs` | Timeout duplicado (named client 5s **e** linked CTS `CancelAfter(5s)`) — redundância defensiva intencional, espelha o sample da story; qualquer um dos dois garante o contrato | Sem ação (documentado) |
| 3 | INFO | `Tools/FifaActionTools.cs` | Caminho "não configurado" retorna antes do `LogInformation` da tool — invocação sem rastro no log da tool (o cenário é ambiente sem F5+ Fase B; aceito) | Sem ação |

Nenhum finding CRITICAL ou HIGH.

## Verificações executadas

- **Conformidade AC-2..AC-6/AC-9/AC-10:** Description PT-BR exata; parâmetros opcionais `= null`; payload com os 6 campos `[JsonPropertyName]` exatos; `N8N_ALERT_WEBHOOK_URL` via `IConfiguration`; mensagem específica de ambiente não configurado (AC-3) viabilizada por `IsConfigured` (extensão documentada no Dev Agent Record); 8 tools com discriminador (teste: exatamente 7 `readOnlyHint=true` + 1 action = `criar_alerta_ingresso`).
- **Regra de ouro (Inv 1):** grep formal — zero `INSERT/UPDATE/DELETE/EXEC/SqlConnection/SqlCommand/Dapper` no notifier; tool não referencia `IFifaQueryRepository`.
- **PII (Inv 7):** `GetRawOid()` aparece apenas na montagem do payload; todos os logs usam `GetMaskedOidForLog()` ou só `correlationId`.
- **Fire-and-forget:** try/catch abrangente, non-2xx → `false`, nunca re-throw; linked CTS encadeado ao token do host (paridade com `N8nWebhookNotifier` F4, lido lado a lado).
- **DI/lifetimes:** notifier Scoped + named client via factory (handler pooled); `ConfigureTestServices` sobrepõe corretamente o registro real nos testes de integração (último registro vence — provado pelos testes).
- **Logging estruturado:** parâmetros nomeados, sem interpolação de input do usuário no template.
- **Testes:** 70/70 McpServer (8 novos), 11/11 Gateway, 50/50 Functions; build 0 warnings 0 errors (re-executado após o fix do finding 1).
- **Material didático (@analyst):** diff inspecionado — append-only confirmado nos dois READMEs; fatos conferem com os Achados Task 0 (nomes de nós, FQDN, transporte).

## Veredito

**APROVADO para InReview** — sem issues bloqueantes; pendências de runtime (Tasks 5.2/6/7.2) registradas na story e fora do escopo de código.
